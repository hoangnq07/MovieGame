const WallOfFame = ({ category }) => {
    const API_URL = `https://your-backend-url.onrender.com/api/favorites/${category}`;
  
    // Hàm fetch, add, delete vẫn giữ nguyên
    // Nhưng sẽ lấy data theo danh mục (movies, manga, books, games)
  };
export default WallOfFame;